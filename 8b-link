#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
int main(intargc, char* argv[])
{
  intsl = symlink(argv[1], argv[2]);
  if (sl == 0)
 {
   printf("Soft Link created succuessfuly");
  }
  return 0;
}
